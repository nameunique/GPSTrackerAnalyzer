name: gps_tracker_analyzer
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.5
  downloadsfolder: ^1.1.0
  flutter_bloc: ^9.1.1
  get_it: ^8.1.0
  logger: ^2.6.1
  flutter_blue_plus: ^1.35.5
  permission_handler: ^12.0.0
  fl_chart: ^0.70.2
  equatable: ^2.0.7


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
